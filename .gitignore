models/
piper_models/
rnnoise_models/

# 비밀/환경설정 (절대 커밋 금지 — 여기에 API 키 등이 들어감)
.env

# 파이썬 / 가상환경
__pycache__/
*.pyc
.venv/

# 런타임 생성물
_stt_warmup.wav

# 학습 데이터 (용량 큼 — 녹음 파일은 로컬/파이에만 둔다)
training/data/

# 학습 전용 venv (PC 로컬, 파이엔 없음)
.venv-train/
# 대본 생성용 venv (WSL, datasets 만 들어있음 — build_prompts.py 실행용)
.venv-tools/
# PC GPU LLM 서버용 venv (WSL, llama-cpp-python CUDA 빌드 — docs/PC_LLM.md)
.venv-llm/
.venv-llm-build.log
.pc_llm_server.log
.pc_llm_server.pid
# 볼트 검색 서버 (PC 전용 — docs/VAULT_RAG.md). 인덱스는 볼트에서 다시 만들면 된다.
.vault_server.log
.vault_server.pid
.vault_index.pkl
training/funasr_data/
training/*.json
# 공개 코퍼스에서 거른 문장 캐시 (build_prompts.py 가 다시 만든다)
training/corpus_cache.tsv
