models/
piper_models/
rnnoise_models/

# 비밀/환경설정 (절대 커밋 금지 — 여기에 API 키 등이 들어감)
.env

# 파이썬 / 가상환경
__pycache__/
*.pyc
.venv/

# 런타임 생성물
_stt_warmup.wav

# 학습 데이터 (용량 큼 — 녹음 파일은 로컬/파이에만 둔다)
training/data/

# 학습 전용 venv (PC 로컬, 파이엔 없음)
.venv-train/
training/funasr_data/
training/*.json
